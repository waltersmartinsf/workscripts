%module applying a butterworth low-pass filter to whatever data being put in

function [OUT] = bss_lp(DATA,PARAM)


% TPTR = 'heursure'; %threshold selection rule for wavelet filter
% SORH = 's';       %soft or hard thresholding
% SCAL = 'mln';     %defines multiplicative threshold rescaling
% N = 3;            %level of decomposition
% WNAME = 'db8';    %mother wavelet function
% 
% OUT2 = wden(DATA(:,1),TPTR,SORH,SCAL,N,WNAME);
% 
% 
% FFT = fft(DATA(:,1));




